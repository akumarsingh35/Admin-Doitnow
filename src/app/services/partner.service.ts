/**
 * @deprecated Legacy entry — use `AdminPartner` / `AdminPartnerService` from `admin-partner.service`.
 * Kept re-exported for any stale imports; new code should import from `admin-partner.service` only.
 */
export {
  type AdminPartner as Partner,
  AdminPartnerService,
} from './admin-partner.service';
